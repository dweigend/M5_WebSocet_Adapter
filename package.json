{
  "name": "m5-websocket-adapter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "server": "bun run src/server/index.ts",
    "lint": "biome check .",
    "check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
    "build": "vite build",
    "test": "vitest run && bun run verify:integration",
    "simulate:device": "bun run scripts/simulate-device.ts",
    "verify:integration": "bun run scripts/verify-integration.ts",
    "tools:setup": "uv sync --group dev --group firmware",
    "setup:controller": "uv run python scripts/setup-controller-tui.py",
    "serial:probe": "uv run python scripts/probe-m5stick-serial.py",
    "python:format": "uv run --group dev ruff format scripts/setup-controller-tui.py scripts/setup_controller",
    "python:lint": "uv run --group dev ruff check scripts/setup-controller-tui.py scripts/setup_controller",
    "python:typecheck": "uv run --group dev ty check scripts/setup-controller-tui.py scripts/setup_controller",
    "firmware:build": "uv run --group firmware pio run -d firmware",
    "firmware:upload": "uv run --group firmware pio run -d firmware --target upload",
    "firmware:monitor": "uv run --group firmware pio device monitor -d firmware --baud 115200"
  },
  "dependencies": {
    "lucide-svelte": "^1.0.1",
    "svelte": "^5.55.8",
    "three": "^0.184.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.15",
    "@sveltejs/adapter-auto": "^7.0.1",
    "@sveltejs/kit": "^2.60.1",
    "@sveltejs/vite-plugin-svelte": "^7.1.2",
    "@types/bun": "^1.3.14",
    "@types/three": "^0.184.1",
    "svelte-check": "^4.4.8",
    "typescript": "^6.0.3",
    "vite": "^8.0.13",
    "vitest": "^4.1.6"
  }
}
