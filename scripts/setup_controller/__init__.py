"""Support modules for the M5StickC Plus2 setup TUI.

The package keeps the prompt-based setup assistant split by responsibility:
local metadata, command execution, serial provisioning, and hub checks. It is
not a reusable public API; the stable user entrypoint is `setup-controller-tui.py`.
"""
